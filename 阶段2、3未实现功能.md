核实结论：文档中“阶段2、3已完成”的表述偏乐观，实际均存在缺口。

## 阶段2：外部资料冲突处理

已实现：冲突模型、SQLite 持久化、详情页提示，以及“保留本机/采用云端/解除关联”操作。

未完整实现：

1. 未解决冲突仍可能覆盖云端资料
   全量同步先记录冲突，随后仍无条件上传本机资料，未检查冲突是否待处理：[WebDavFullSyncService.cs (line 108)](D:/Study/Projects/FireflyGameManager/GameManager.App/Services/WebDavFullSyncService.cs:108)、[上传位置 (line 176)](D:/Study/Projects/FireflyGameManager/GameManager.App/Services/WebDavFullSyncService.cs:176)。
2. 外部资料同步日志不完整
   目前只有“启动拉取发现冲突”写入 `external-metadata` 日志：[WebDavCloudMetadataPullService.cs (line 52)](D:/Study/Projects/FireflyGameManager/GameManager.App/Services/WebDavCloudMetadataPullService.cs:52)。
   缺少上传、下载成功、失败及全量同步冲突日志。
3. 缺少关键流程测试
   没有覆盖：
   - 未解决冲突不得上传覆盖云端。
   - 解除关联解决冲突。
   - 解决后下一次同步上传所选版本。
   - 日志脱敏及完整记录。

## 阶段3：批量与收藏体验

### 3.1 批量匹配

未完整实现：

1. 没有高置信度判断
   当前直接选择搜索结果第一项：[ManageGameLibraryViewModel.cs (line 97)](D:/Study/Projects/FireflyGameManager/GameManager.App/ViewModels/ManageGameLibraryViewModel.cs:97)。
2. 不能查看或切换多个候选
   UI 只显示第一候选的文字，没有候选列表、候选详情或手动换项。
3. 没有逐游戏重试
   失败原因会显示，但只能重新执行整批匹配。
4. 未复用字段导入策略
   批量应用直接写入完整外部资料快照，没有使用 `MetadataImportOptions`，也不处理封面下载和字段选择：[ManageGameLibraryViewModel.cs (line 142)](D:/Study/Projects/FireflyGameManager/GameManager.App/ViewModels/ManageGameLibraryViewModel.cs:142)。
5. 未接入新的自愈降级查询
   批量流程仍调用 `GetDetailsAsync`，没有携带游戏名执行 legacy large 降级；详情接口404时会得到“候选详情不可用”。

### 3.2 Bangumi 收藏字段 UI

仅实现：收藏状态、评分、短评。

未实现或不完整：

1. 私有备注未实现。
2. 游玩进度未实现。
3. 账号失效时整个收藏区直接隐藏，而不是显示重连提示。
4. 无法清空远端评分或短评
   评分为0、评论为空时不会发送对应字段：[BangumiApiClient.cs (line 222)](D:/Study/Projects/FireflyGameManager/GameManager.App/Services/BangumiApiClient.cs:222)。
5. 当前收藏模型本身只有状态、评分和评论：[BangumiCollectionState.cs (line 13)](D:/Study/Projects/FireflyGameManager/GameManager.App/Models/BangumiCollectionState.cs:13)。

### 3.3 搜索体验优化

已实现：CJK legacy fallback、符号/空格/全半角归一化、基础多关键词匹配。

未完整实现：

1. 搜索结果没有别名字段。
2. 搜索结果没有开发商辅助信息。
3. 排序只比较原名、本地化名和显示名，不会使用别名。
4. 多关键词只是移除分隔符后整体匹配，不是真正的分词、乱序关键词匹配。

因此，阶段2属于“主体完成但存在同步安全缺口”，阶段3属于“最小闭环完成，方案要求尚未完整交付”。此次仅核实，未修改代码。