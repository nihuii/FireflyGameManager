<UserControl x:Class="GameManager.App.Views.GameLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="TransparentCardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TextIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LibraryOverlayContextMenuStyle" TargetType="ContextMenu">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{DynamicResource PanelBrush}"
                                BorderBrush="{DynamicResource LineBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="6">
                            <Border.Effect>
                                <DropShadowEffect Color="Black"
                                                  BlurRadius="18"
                                                  ShadowDepth="3"
                                                  Opacity="0.18" />
                            </Border.Effect>
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LibraryOverlayMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="112" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="MenuItemBorder"
                                Background="Transparent"
                                CornerRadius="6">
                            <ContentPresenter ContentSource="Header"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuItemBorder" Property="Background" Value="{DynamicResource SurfaceBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerOverlayMenuItemStyle"
               TargetType="MenuItem"
               BasedOn="{StaticResource LibraryOverlayMenuItemStyle}">
            <Setter Property="Foreground" Value="#B83A3A" />
        </Style>
    </UserControl.Resources>

    <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="280" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="全部游戏"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextBrush}" />
                <TextBlock Text="{Binding Games.Count, StringFormat={}{0} 个本地条目}"
                           Style="{StaticResource MutedTextStyle}"
                           Margin="0,4,0,0" />
            </StackPanel>
            <TextBox Grid.Column="1"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource TextBoxStyle}"
                     VerticalAlignment="Center"
                     ToolTip="搜索游戏名称" />
        </Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Games}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Viewbox Width="{Binding DataContext.CardWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                 Height="{Binding DataContext.CardHeight, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                 Stretch="Fill"
                                 Margin="0,0,20,24">
                        <Grid x:Name="CardRoot"
                              Width="178"
                              Height="244"
                              ClipToBounds="False">
                            <Grid.Clip>
                                <RectangleGeometry x:Name="RoundedCoverClip"
                                                   Rect="0,0,178,244"
                                                   RadiusX="12"
                                                   RadiusY="12" />
                            </Grid.Clip>
                            <Border CornerRadius="8"
                                    ClipToBounds="True">
                                <Border.Effect>
                                    <DropShadowEffect Color="#1E2521"
                                                      BlurRadius="12"
                                                      ShadowDepth="2"
                                                      Opacity="0.14" />
                                </Border.Effect>
                                <Grid Background="{DynamicResource SurfaceBrush}"
                                      ClipToBounds="True">
                                    <TextBlock Text="封面"
                                               Foreground="{DynamicResource MutedTextBrush}"
                                               FontSize="16"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                    <Image x:Name="CoverImage"
                                           Source="{Binding CoverImagePath}"
                                           Stretch="UniformToFill"
                                           RenderTransformOrigin="0.5,0.5">
                                        <Image.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </Image.RenderTransform>
                                    </Image>
                                    <Rectangle VerticalAlignment="Bottom"
                                               Height="88">
                                        <Rectangle.Fill>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#00000000" Offset="0" />
                                                <GradientStop Color="#AA111814" Offset="1" />
                                            </LinearGradientBrush>
                                        </Rectangle.Fill>
                                    </Rectangle>
                                    <TextBlock Text="{Binding Name}"
                                               VerticalAlignment="Bottom"
                                               Margin="14,0,42,15"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="White"
                                               TextTrimming="CharacterEllipsis">
                                        <TextBlock.Effect>
                                            <DropShadowEffect Color="Black"
                                                              BlurRadius="6"
                                                              ShadowDepth="1"
                                                              Opacity="0.45" />
                                        </TextBlock.Effect>
                                    </TextBlock>
                                    <TextBlock Text="{Binding PlayTimeDisplay}"
                                               VerticalAlignment="Top"
                                               HorizontalAlignment="Left"
                                               Margin="12"
                                               Padding="7,4"
                                               FontSize="11"
                                               FontWeight="SemiBold"
                                               Foreground="White"
                                               Background="#66000000"
                                               Visibility="{Binding DataContext.ShowPlayTimeOnCards, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </Grid>
                            </Border>

                            <Button x:Name="CardClickButton"
                                    Command="{Binding DataContext.OpenGameDetailCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource TransparentCardButtonStyle}"
                                    Background="Transparent"
                                    Cursor="Hand" />

                            <Button x:Name="MoreButton"
                                    Width="34"
                                    Height="30"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Bottom"
                                    Margin="0,0,8,10"
                                    Content="..."
                                    Style="{StaticResource TextIconButtonStyle}"
                                    FontSize="16"
                                    FontWeight="Bold"
                                    Opacity="0"
                                    Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    Click="MoreButton_Click">
                                <Button.ContextMenu>
                                    <ContextMenu Style="{StaticResource LibraryOverlayContextMenuStyle}"
                                                 Placement="Top"
                                                 VerticalOffset="-4">
                                        <MenuItem Header="修改"
                                                  Style="{StaticResource LibraryOverlayMenuItemStyle}"
                                                  Command="{Binding PlacementTarget.Tag.EditGameCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="置顶"
                                                  Style="{StaticResource LibraryOverlayMenuItemStyle}"
                                                  Command="{Binding PlacementTarget.Tag.PinGameCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="删除"
                                                  Style="{StaticResource DangerOverlayMenuItemStyle}"
                                                  Command="{Binding PlacementTarget.Tag.DeleteGameCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    </ContextMenu>
                                </Button.ContextMenu>
                            </Button>
                        </Grid>
                        </Viewbox>
                        <DataTemplate.Triggers>
                            <Trigger SourceName="CardRoot" Property="IsMouseOver" Value="True">
                                <Setter TargetName="MoreButton" Property="Opacity" Value="1" />
                            </Trigger>
                            <EventTrigger SourceName="CardRoot" RoutedEvent="Mouse.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="CoverImage"
                                                         Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                         To="1.08"
                                                         Duration="0:0:0.22" />
                                        <DoubleAnimation Storyboard.TargetName="CoverImage"
                                                         Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                         To="1.08"
                                                         Duration="0:0:0.22" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger SourceName="CardRoot" RoutedEvent="Mouse.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="CoverImage"
                                                         Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                         To="1"
                                                         Duration="0:0:0.28" />
                                        <DoubleAnimation Storyboard.TargetName="CoverImage"
                                                         Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                         To="1"
                                                         Duration="0:0:0.28" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
